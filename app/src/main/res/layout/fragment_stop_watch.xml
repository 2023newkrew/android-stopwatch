<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/stop_watch_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/time_layout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/black"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toTopOf="@id/scroll_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/sec_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/black"
            android:textColor="@android:color/white"
            android:textSize="@dimen/text_size_large"
            tools:text="12" />

        <TextView
            android:id="@+id/milli_sec_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@color/black"
            android:textColor="@android:color/white"
            android:textSize="@dimen/text_size_default"
            tools:text="12" />
    </androidx.appcompat.widget.LinearLayoutCompat>

    <ProgressBar
        android:id="@+id/check_back_progress_bar"
        android:layout_width="@dimen/progress_size_default"
        android:layout_height="@dimen/progress_size_default"
        android:background="@drawable/shape_progress_background_time"
        android:indeterminateOnly="false"
        android:progressDrawable="@drawable/shape_progress_drawable_check_back"
        app:layout_constraintBottom_toTopOf="@id/scroll_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/check_front_progress_bar"
        android:layout_width="@dimen/progress_size_default"
        android:layout_height="@dimen/progress_size_default"
        android:background="@android:color/transparent"
        android:indeterminateOnly="false"
        android:progressDrawable="@drawable/shape_progress_drawable_check_front"
        app:layout_constraintBottom_toTopOf="@id/scroll_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/time_progress_bar"
        android:layout_width="@dimen/progress_size_default"
        android:layout_height="@dimen/progress_size_default"
        android:background="@android:color/transparent"
        android:indeterminateOnly="false"
        android:progressDrawable="@drawable/shape_progress_drawable_time"
        app:layout_constraintBottom_toTopOf="@id/scroll_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:id="@+id/scroll_view"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/height_scroll_view"
        android:padding="@dimen/margin_default"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/play_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <androidx.appcompat.widget.LinearLayoutCompat
            android:id="@+id/lap_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/refresh_button"
        android:layout_width="@dimen/button_size_default"
        android:layout_height="@dimen/button_size_default"
        android:contentDescription="@string/description_refresh_button"
        android:src="@drawable/icon_refresh"
        android:visibility="invisible"
        app:backgroundTint="@color/pink"
        app:fabCustomSize="@dimen/button_size_default"
        app:layout_constraintBottom_toBottomOf="@id/play_button"
        app:layout_constraintEnd_toStartOf="@id/play_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/play_button" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/play_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_default"
        android:background="@drawable/layer_list_play"
        android:contentDescription="@string/description_play_button"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/lap_button"
        android:layout_width="@dimen/button_size_default"
        android:layout_height="@dimen/button_size_default"
        android:contentDescription="@string/description_lap_button"
        android:src="@drawable/icon_lap"
        android:visibility="invisible"
        app:backgroundTint="@color/pink"
        app:fabCustomSize="@dimen/button_size_default"
        app:layout_constraintBottom_toBottomOf="@id/play_button"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/play_button"
        app:layout_constraintTop_toTopOf="@id/play_button" />
</androidx.constraintlayout.widget.ConstraintLayout>